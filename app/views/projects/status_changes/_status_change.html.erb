<div class="bg-gray-100 p-4 rounded-lg">
  <p class="text-sm text-gray-600"><%= status_change.user.name %> changed status to <strong><%= status_change.status.humanize %></strong> on <span class="font-medium"><%= status_change.created_at.strftime("%B %d, %Y") %></span></p>
</div>
